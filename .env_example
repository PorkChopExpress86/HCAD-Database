# Example environment variables for HCAD Database loader
# Copy this file to `.env` and update values before running the loader.

# SQLAlchemy-compatible database URI.
# For Docker Postgres:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hcad
# Optional: API key for the HTTP bridge (recommended for security). Set in .env when using the bridge service.
MCP_BRIDGE_API_KEY=
# For local SQLite:
# DATABASE_URL=sqlite:///./data/hcad.db

# Postgres configuration (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=hcad
POSTGRES_PORT=5432

# Recommended number of workers (tune for your machine)
WORKERS=4

# Run mode
ENV=development
